# tiny-color-tools
tiny-color-tools
# tiny-color-tools
Small utilities for generating and converting colors.

## Files
- `random_color.py` — generates a random HEX color.
- `hex_to_rgb.py` — converts HEX color into RGB tuple.
- `palette.txt` — sample palette file.
